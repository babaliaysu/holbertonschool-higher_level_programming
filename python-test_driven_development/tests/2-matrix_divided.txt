The ``2-matrix_divided`` module
================================

This module supplies one function, ``matrix_divided(matrix, div)``.

Basic usage:
------------

First, import the function:
    >>> matrix_divided = __import__('2-matrix_divided').matrix_divided

Simple case of dividing a matrix by an integer:
    >>> matrix = [[1, 2, 3], [4, 5, 6]]
    >>> matrix_divided(matrix, 3)
    [[0.33, 0.67, 1.0], [1.33, 1.67, 2.0]]

Dividing by a float:
    >>> matrix_divided([[10, 20], [30, 40]], 2.5)
    [[4.0, 8.0], [12.0, 16.0]]

Edge Cases and Errors:
----------------------

Checking if div is zero:
    >>> matrix_divided([[1, 2], [3, 4]], 0)
    Traceback (most recent call last):
        ...
    ZeroDivisionError: division by zero

Checking if div is not a number:
    >>> matrix_divided([[1, 2], [3, 4]], "two")
    Traceback (most recent call last):
        ...
    TypeError: div must be a number

Checking for non-equal row sizes:
    >>> matrix_divided([[1, 2], [3, 4, 5]], 2)
    Traceback (most recent call last):
        ...
    TypeError: Each row of the matrix must have the same size

Checking for non-matrix input (not a list):
    >>> matrix_divided("not a matrix", 2)
    Traceback (most recent call last):
        ...
    TypeError: matrix must be a matrix (list of lists) of integers/floats

Checking for non-numeric elements in matrix:
    >>> matrix_divided([[1, "2"], [3, 4]], 2)
    Traceback (most recent call last):
        ...
    TypeError: matrix must be a matrix (list of lists) of integers/floats
